# バンド情報の取得フローと推定ロジック

本ドキュメントでは、本アプリケーションがどのようにモバイルネットワークのバンド情報を取得し、多重のフォールバックレイヤーを通じて高い精度と信頼性で推定しているかについて説明します。

## 1. データソースの優先順位（階層構造）

アプリケーションは、利用可能な情報を最大限詳細かつ正確に提供するため、以下の順序でデータソースを優先します。

### 第1レイヤー: PhysicalChannelConfig (PCC)
- **主要ソース**: Shizukuを使用してシステムの `IPhoneStateListener` にアクセスします。
- **詳細**: キャリアアグリゲーション（CA）のセカンダリセルを含む、すべての有効な物理チャネルのリアルタイム情報を取得します。
- **制限**: Shizukuが実行され、許可されている必要があります。一部のデバイスでは、PCCがCAメンバーのMCC/MNC/TAC/CellIDを報告しない場合があります。

### 第2レイヤー: ServiceState (フォールバック 1)
- **主要フォールバック**: PCCが利用できない場合、またはセルの識別情報が不足している場合に使用されます。
- **メカニズム**: Android標準の `TelephonyManager.getServiceState()` および `NetworkRegistrationInfo` を使用します。
- **詳細**: プライマリサービングセルの確実な MCC/MNC/TAC/CellID を提供します。

### 第3レイヤー: CellInfo (フォールバック 2)
- **補助フォールバック**: 補助的な信号強度やチャネルの照合に使用されます。
- **メカニズム**: `TelephonyManager.getAllCellInfo()` を使用します。
- **詳細**: 周辺セルの識別や、PCI/ARFCNを比較することによるCAセカンダリメンバーへの信号値の紐付けに役立ちます。

---

## 2. バンド推定ロジック

システムがバンド番号を明示的に報告しない場合（`0` として報告される場合）、アプリケーションはチャネル周波数（ARFCN）を使用してバンドを推定します。

### 推定フロー
1. **PCC/SS チェック**: システムが 0 より大きいバンド番号を報告している場合は、それを直接使用します。
2. **5G (NR) 判定**:
   - ネットワークタイプが NR、または `nrarfcn` が存在する場合：
     - `nrarfcn` を使用してバンド番号を検索します（例: n78, n257）。
     - 周波数が Sub-6GHz 帯域（620000-640000）の場合は、`*` マーカーを付与します。
     - システムからの報告ではなく ARFCN から推定された場合は、`?` サフィックスを付与します。
3. **4G (LTE) 判定**:
   - 5G が検出されず、`earfcn` が存在する場合：
     - `earfcn` を使用して LTE バンド番号を検索します（例: B1, B3, B19）。
     - 推定された場合は `?` サフィックスを付与します。

### ARFCN 変換
- **LTE**: E-UTRA バンドの標準範囲を使用します。
- **NR**: NR バンド（FR1/FR2）の標準範囲を使用します。これには、ミリ波帯（n258, n260, n261）の拡張範囲も含まれます。

---

## 3. データの統合とエンリッチメント

`TelephonyBandDataManager` は、これらのソースを統合する役割を担います。

1. **セル情報の補完（Enrichment）**: プライマリサービングセル（`ServiceState`）からの TAC、Cell ID、PLMN 情報を `PhysicalChannelConfig` の CA メンバーに自動的に適用し、接続の全体像を構築します。
2. **シグナルマッチング**: `CellInfo` からの信号値（RSRP/RSRQ/SINR）を PCI と ARFCN で照合し、物理チャネルに紐付けます。これにより、セカンダリキャリアでもシグナルバーを表示可能にします。
3. **キャッシュ制御**: 無線状態の瞬間的な変化による表示のチラつきを抑え、一貫性を保つために 3 秒間のキャッシュを使用します。

---

## 4. 発生しうる表示パターン

デバイスの状態や権限設定により、バンド情報の表示は以下のようになります。

| シナリオ | PCC (Shizuku) | ServiceState | ARFCN情報 | 表示結果の例 | 備考 |
| :--- | :---: | :---: | :---: | :--- | :--- |
| **理想的な状態** | OK | OK | あり | `n78` / `B1` | すべてのソースが一致し、正確な表示。 |
| **Shizuku未起動/権限なし** | NG | OK | あり | `B3` (SS経由) | プライマリセルのみ表示。CA情報は取得不可。 |
| **PCCでBand情報欠損** | OK (Band=0) | OK | あり | `n78?` / `B1?` | PCCからチャネル情報(ARFCN)のみ取れる場合に推定。 |
| **キャリアアグリゲーション (CA)** | OK | OK | あり | `B1` + `B3` + `B42` | PCCによりセカンダリのバンド・帯域幅を表示。 |
| **5G ENDC (LTE+NR)** | OK | 5G(NSA)判定 | あり | `B3` + `n78` | 4Gをアンカーとして5Gを追加。PCCで両方の情報が必要。 |
| **5G ENDC 待機中** | OK | 5G判定 | NRARFCNなし | `B3` (LTEのみ) | 通信タイプは5Gと出るが、NR接続がないためLTEのみ表示。 |
| **PCC/SS共にBand取得不可** | NG/Band=0 | NG/Band=0 | あり (CellInfo等) | `B19?` | 最終手段としてARFCN推定値のみを表示。 |
| **完全な情報欠損** | NG | NG | なし | `?` | 情報を取得できる手段がない場合。 |

- **`?` 付き表示**: システム（OS）が明示的なバンド番号を返さず、アプリが周波数から「おそらくこのバンドである」と推測したことを示します。
- **CA（キャリアアグリゲーション）**: PCCが動作している場合のみ可能ですが、セカンダリのセル情報の多くはプライマリセル（ServiceState）の情報で補完されます。
